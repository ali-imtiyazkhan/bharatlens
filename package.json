{
  "name": "bharatlens",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "start": "turbo run start",
    "lint": "turbo lint",
    "db:push": "turbo run db:push",
    "db:generate": "turbo run db:generate",
    "db:seed": "pnpm --filter @bharatlens/db prisma db seed"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "turbo": "^1.13.3",
    "typescript": "^5.4.5"
  },
  "packageManager": "pnpm@8.15.6",
  "engines": {
    "node": ">=18"
  }
}
