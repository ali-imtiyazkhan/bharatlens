services:
  - type: web
    name: bharatlens-api
    env: node
    plan: free
    region: oregon 
    rootDir: .
    buildCommand: pnpm install && pnpm exec turbo run db:generate build --filter=api && pnpm --filter @bharatlens/db run db:push -- --accept-data-loss
    startCommand: pnpm --filter api start
    envVars:
      - key: NODE_VERSION
        value: 18
      - key: PORT
        value: 3001
      - key: DATABASE_URL
        fromDatabase:
          name: bharatlens-db
          property: connectionString
      - key: FRONTEND_URL
        value: https://bharatlens-web.vercel.app
      - key: GEMINI_API_KEY
        sync: false

databases:
  - name: bharatlens-db
    plan: free
    region: oregon
